import { Box, Stack, Typography } from "@mui/material";
import React from "react";

const HeroBanner = () => {
  return (
    <Box>
    <Typography>
     Fitness Club
    </Typography>
    </Box>
  )
};

export default HeroBanner;
